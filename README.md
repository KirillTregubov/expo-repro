## Reproduction of issue with `expo-asset` or `expo-image`

Steps to reproduce:

1. Clone this repo

2. Run `npm install` in the root directory

3. Run `npm run ios` in the root directory to build and run the iOS app

Once the app is running, you should see the following screen:

![Screenshot of the app](screenshot.png)
